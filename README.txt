1. The poi_id.py file can be run on its own and can be tested with test.py
2. The following intermidate processes where done in a separate code that can 
    be run on thier own. The outocome of those codes is hard coded in the poi_id.py
        * The code for feature selection is provided in the Feature_slection_final.txt
        * The code for creating a new feature is provided in New_feature.txt
3. Just for completeness sake, a code for counting the missing data in each feature is provided 
   in Counter_for_NaN.txt